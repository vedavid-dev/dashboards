apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vedavid
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: vedavid-dashboards-k8s
    files:
      - cluster-overview.yaml
